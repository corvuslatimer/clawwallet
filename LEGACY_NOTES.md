# Legacy Notes

Legacy monolithic/retired files were removed from the repo root during cleanup.
Backups are preserved under `.trash/<date>/repo-cleanup/` locally.

Removed from active root:
- `old.js`
- `readme.js`
- `legacy-core.js`
- `logs.txt`
- `test.js`

Use `cli.js` + modular files in `pump/`, `solana/`, and `utils/` as current source of truth.
